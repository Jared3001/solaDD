# Web front end + the DD pipeline's own deps (pinned to the working venv).
Flask>=3.0,<4
gunicorn>=21.2
# OM extraction calls the Gemini REST API directly via `requests` (below) — no SDK.
# ai_scraper (--source ai, default) calls Firecrawl + Gemini REST APIs via urllib.request —
# no extra pip deps; set FIRECRAWL_API_KEY + GEMINI_API_KEY env vars.

# pipeline deps (mirror build/sources requirements)
openpyxl==3.1.5
PyYAML==6.0.3
requests==2.34.2

# deal-tracker export -> Google Sheets (dormant until TRACKER_SHEET_ID + creds set)
gspread>=6.1,<7
google-auth>=2.30,<3

# transitive — pinned for reproducible Railway builds
certifi==2026.5.20
charset-normalizer==3.4.7
idna==3.18
urllib3==2.7.0
et_xmlfile==2.0.0
